# 讲解口播稿:这个项目是干嘛的 + A\* / CBS / MA-CBS

这是一份**照着念就能讲清楚**的口语稿,不是技术文档(技术文档看 [ALGORITHMS.md](ALGORITHMS.md))。分四段:项目是干嘛的、A\*、CBS、MA-CBS,讲的时候可以按需要停顿、举例、配合演示视频([README.md](README.md) 里有)。

---

## 一、这个项目是干嘛的

这个项目是我们上 DTU 02285《人工智能与多智能体系统》这门课的编程作业,说白了就是写一个能自动"玩推箱子游戏"的机器人大脑。

具体场景是这样的:有一张仓库地图,上面有墙、有几个机器人、还有几个箱子,每个机器人和箱子都有自己的颜色。规则很简单——机器人只能推或者拉跟自己同色的箱子,碰不了别的颜色的箱子。地图上还标了几个目标点,每个目标点会要求"必须是某个字母的箱子"或者"必须是几号机器人"停在上面。

我们要做的事,就是写一个程序:读懂这张地图,然后**自动算出**每一步、每个机器人该做什么动作——往哪走、要不要推/拉箱子——最后把所有箱子和机器人都送到该去的目标点,而且整个过程中谁也不能撞上谁。

这里面难的地方分两层:

- 第一层,**一个机器人自己怎么走最快最省事**——这个用 **A\*** 算法解决。
- 第二层,**好几个机器人同时在跑**,每个人各自的最优路线放一起大概率会打架——这个用 **CBS** 和 **MA-CBS** 来解决。

下面就按这个顺序,一层一层讲。

---

## 二、A\*:一个机器人怎么走最快

最笨的办法是"地毯式搜索"——一圈一圈往外找,不管方向,这个叫 BFS,肯定能找到最短路,但特别慢,因为它完全不知道该往哪边使劲。

A\* 聪明在哪呢?它每走一步都同时看两件事:**我已经走了多少步**,加上**我猜还要走多少步才能到目标**。这两个数加起来,谁的和最小,就优先往哪边走。这个"猜还要走多少步"就是启发函数——最简单的猜法就是算横竖走的格子数(曼哈顿距离)。这个猜测只要不高估真实距离,A\* 就能保证找到的路一定是最短的,而且比"地毯式"的 BFS 快得多。

我们这个项目在这基础上还多做了个优化:因为地图上有好几种颜色的机器人和箱子,不同颜色其实是各干各的、互不相关,所以我们猜"还要走多少步"的时候,不是把所有颜色的路程加在一起猜,而是**按颜色分组算,每组单独猜完,取最慢的那一组**——因为不同颜色可以同时动,整体花的时间取决于最慢的那一组,这样猜得更准,搜索也更快。

---

## 三、CBS:好几个机器人一起跑,怎么不撞车

A\* 解决的是"一个人怎么走最快",但我们这关卡里有好几个机器人。如果每个人各自用 A\* 规划出自己最快的路,再放到一起——大概率会撞车。撞车主要三种情况:

1. 两个人同一时刻抢同一个格子;
2. 两个人正好对着走、擦肩交换位置(你去我这、我去你那);
3. 一个人紧跟着另一个人,结果撞上了对方刚让开的位置。

CBS 的思路很朴素:**先各自规划,撞了再说**。检测到两个人要撞了,就试两个方案——要么让第一个人绕开这个时间点这个位置,要么让第二个人绕开,分别单独给**这一个人**重新规划一次路,其他人的路完全不用动。这样就出来两个分支,每个分支再检查一遍还撞不撞,撞的话继续往下分。整个过程就像一棵不断长出新分支的树,每次都挑"目前看起来总路程最短"的那个分支往下检查,直到找到一个所有人路径都不冲突的方案——这个方案就保证是最优的。

这么做的好处是:**不用把所有人绑在一起搜**,谁撞车就单独管谁,搜索空间小很多,比"一上来就把所有机器人当成一个整体去搜"快得多。

---

## 四、MA-CBS:什么时候干脆别各自规划了,直接联手

但 CBS 有个死穴:如果两个机器人是卡在一条只能过一个人的窄路上,天生就得"你等我先走、我再让你走",那不管怎么加约束,这两个人就会来来回回地冲突,树就一直分裂,越分越大,永远搜不完。

MA-CBS 的解法是:**给每一对机器人记一个"红脸次数"**——如果这两个人反反复复冲突,冲突次数攒到一个阈值,就不再各自规划、来回加约束了,直接把这两个人**绑成一个整体**,当一个联合体重新搜一次路。绑一起之后,搜索天然就会算出"你先让开、我过去、你再回来"这种真正需要协同配合的方案,而不是死磕"到底该谁让谁"。

代价是什么呢?绑得越多,搜索空间就是几个人动作数**相乘**,会越搜越大,所以这个阈值不能设得太激进——窄路走廊这种地方,冲突几次就该合并;开阔空间大家本来就不太会撞,合并反而是浪费,应该让他们各自搜,能不合并就不合并。我们项目里实测过一个关卡叫 `Happyya.lvl`,就是典型的"必须合并才能解出来"的例子——单纯 CBS 死活搜不出解,一合并立刻就出来了。

---

## 五、一句话总结

**A\* 管一个人怎么走最快,CBS 管多个人怎么避免各扫门前雪撞上车,MA-CBS 管什么时候干脆别各扫门前雪了、直接联手规划。**

需要更细的实现细节(具体是哪个类、哪一行代码做的),看 [ALGORITHMS.md](ALGORITHMS.md);想看效果,[README.md](README.md) 里有演示动图和本地跑起来的命令。
